# Bazy-Danych-2
elo

# Virtualenv HOWTO
Create: `virtualenv venv` 

Activate: `. venv/bin/activate`

Deactivate: `deactivate`

# Installation
* Create a virtualenv
* Activate it
* `pip install -r requirements.txt`

